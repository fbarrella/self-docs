================================================================================
self-docs - Development Progress Log
Append-only. One entry per completed task from DEVELOPMENT_PLAN.md.
Generated by the progress-log skill.
================================================================================

--------------------------------------------------------------------------------
[2026-09-17T16:45:00Z] TASK T0.0 - Analysis and development planning
--------------------------------------------------------------------------------
Phase      : 0 - Foundation & Contracts
Status     : DONE
Summary    : Analyzed PRD.md and DESIGN.md, produced DEVELOPMENT_PLAN.md with
             28 tasks across 7 phases (0-6) plus a recommended implementation
             order and critical-path notes. Created the progress-log skill to
             standardize task records in progress.txt.
Files      : DEVELOPMENT_PLAN.md, .opencode/skills/progress-log/SKILL.md,
             progress.txt
Verification: Plan cross-checked against PRD sections 3.1-3.4, 5 and 6, and
             DESIGN.md sections 2-3; all core features mapped to tasks.
Next       : T0.1 - Repository scaffolding and tooling

--------------------------------------------------------------------------------
[2026-09-17T16:52:00Z] TASK T0.1 - Repository scaffolding and tooling
--------------------------------------------------------------------------------
Phase      : 0 - Foundation & Contracts
Status     : DONE
Summary    : Created the monorepo layout with a Go 1.27 backend module
             (Gin-ready internal package skeleton and migrations dir) and a
             React 19 + Vite + TypeScript frontend. Added root and per-project
             tooling: .gitignore, .editorconfig, README, oxlint + Prettier
             config, and env example.
Files      : backend/go.mod, backend/cmd/server/main.go,
             backend/internal/{api,config,db,handler,model,repository,router,
             service}/, backend/migrations/,
             frontend/package.json, frontend/index.html,
             frontend/src/{main.tsx,App.tsx,index.css},
             frontend/.prettierrc.json, frontend/.prettierignore,
             frontend/.gitignore, frontend/.env.example, frontend/README.md,
             .gitignore, .editorconfig, README.md, deploy/, docs/
Verification: `gofmt -l . && go vet ./... && go build ./...` -> GO_OK;
             `npm run lint` -> clean; `npm run format:check` -> all files
             compliant; `npm run build` -> built in 197ms (tsc + vite).
Next       : T0.2 - Shared API contract and data model design

--------------------------------------------------------------------------------
[2026-09-17T19:48:45Z] TASK T0.2 - Shared API contract and data model design
--------------------------------------------------------------------------------
Phase      : 0 - Foundation & Contracts
Status     : DONE
Summary    : Froze the persistence model and REST contract before
             implementation. data-model.md defines documents, tags,
             document_tags, activity_logs and settings with enums, checks,
             indexes, FTS strategy (simple dictionary, weighted tsvector) and
             private-isolation invariants. api.md defines all endpoints,
             payloads, error codes, pagination, auth/session flow and the SPA
             route map.
Files      : docs/data-model.md, docs/api.md, README.md
Verification: Cross-checked against PRD 3.1-3.4 (documents, sections, search,
             dashboard), PRD 4 (navigation routes) and PRD 5 (private vault,
             performance); resolved slug-uniqueness and lock/locked naming
             inconsistencies before freeze.
Next       : T1.1 - PostgreSQL schema and migrations

--------------------------------------------------------------------------------
[2026-09-17T20:20:17Z] TASK T1.1 - PostgreSQL schema and migrations
--------------------------------------------------------------------------------
Phase      : 1 - Backend Persistence
Status     : DONE
Summary    : Implemented the initial PostgreSQL schema as reversible SQL
             migrations: document_section and activity_action enums, documents
             (with hierarchy, is_private invariant, and FTS vector trigger),
             tags, document_tags, activity_logs, and settings. Added an
             updated_at trigger and partial unique indexes for slug scoping.
Files      : backend/migrations/0001_init.up.sql,
             backend/migrations/0001_init.down.sql
Verification: Applied against a disposable postgres:16-alpine container with
             `psql -v ON_ERROR_STOP=1`; `\dt` listed all 5 tables, up migration
             is idempotent, negative tests confirmed the slug uniqueness,
             parent-only-project_note, and is_private-derived CHECK constraints,
             FTS trigger matched title/content, updated_at bumped on UPDATE, and
             the down migration dropped all objects cleanly before a successful
             re-apply.
Next       : T1.2 - Backend bootstrap: config, DB pool, migrations runner

--------------------------------------------------------------------------------
[2026-09-17T20:27:01Z] TASK T1.2 - Backend bootstrap: config, DB pool, migrations runner
--------------------------------------------------------------------------------
Phase      : 1 - Backend Persistence
Status     : DONE
Summary    : Stood up the Gin server with env-based config (dotenv fallback,
             fail-fast validation), a pgx connection pool, an embedded SQL
             migration runner tracked in schema_migrations, optional
             master-password seeding, CORS middleware, and a /healthz probe.
             Added graceful shutdown on SIGINT/SIGTERM.
Files      : backend/cmd/server/main.go, backend/internal/config/config.go,
             backend/internal/db/db.go, backend/internal/handler/health.go,
             backend/internal/router/router.go, backend/migrations/migrations.go,
             backend/internal/config/config_test.go,
             backend/migrations/migrations_test.go, backend/.env.example,
             backend/go.mod, backend/go.sum
Verification: `gofmt -l .`, `go vet ./...`, `go build ./...` clean; `go test
             ./...` passes. Ran against a postgres:16-alpine container:
             migrations applied on startup (schema_migrations v1), `/healthz`
             and `/api/healthz` returned 200 with database ok, missing
             DATABASE_URL exited 1 with a clear message, a second start skipped
             applied migrations and did not overwrite a non-empty master hash,
             and SIGTERM logged a clean shutdown.
Next       : T1.3 - Domain models and repository layer

--------------------------------------------------------------------------------
[2026-09-17T20:31:16Z] TASK T1.3 - Domain models and repository layer
--------------------------------------------------------------------------------
Phase      : 1 - Backend Persistence
Status     : DONE
Summary    : Added domain models (section/action enums, Document, DocumentNode,
             SearchResult, Tag, Activity, Pagination) and a slug generator, then
             implemented document, tag, and activity repositories isolating all
             SQL. Public queries hard-filter is_private; search ranks with
             ts_rank/ts_headline over title+content+tags; the update path guards
             hierarchy cycles via a recursive CTE. Fixed two bugs surfaced by the
             integration tests: Create/Update now maintain the denormalized
             is_private column (derived CHECK), and tag listing hides tags
             attached only to private documents.
Files      : backend/internal/model/document.go,
             backend/internal/model/slug/slug.go,
             backend/internal/model/slug/slug_test.go,
             backend/internal/repository/{errors,querier,document_repository,
             tag_repository,activity_repository}.go,
             backend/internal/repository/{repository,document_repository,
             tag_repository,activity_repository}_test.go
Verification: `gofmt -l .` and `go vet ./...` clean; `go test ./...` green
             (repository suite skipped without a DB). Ran the integration suite
             against a disposable postgres:16-alpine via
             `TEST_DATABASE_URL=... go test ./internal/repository/`: all 20
             tests pass, including private-exclusion for list/get/tags/
             popular/search/counts/recent and the cycle-rejection test.
Next       : T2.1 - Documents CRUD API

--------------------------------------------------------------------------------
[2026-09-17T20:37:48Z] TASK T2.1 - Documents CRUD API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Implemented the public documents REST API and wired it into the
             router: POST/GET/GET :id/PUT/DELETE /api/documents plus
             GET /api/documents/tree. Added the standard error envelope and
             repository-error mapping, query-parameter validation, section and
             hierarchy rules, a nullable parent_id so PUT can clear parents,
             and activity logging on create/update/delete. Private sections are
             rejected on public routes (403 / excluded from listings). Fixed
             two bugs found by the HTTP tests: include=content was discarded
             during scanning, and delete logged activity after the row was gone
             (FK violation).
Files      : backend/internal/handler/{response.go,document_handler.go,
             nullable.go,health.go}, backend/internal/router/router.go,
             backend/internal/handler/{handler_test.go,
             document_handler_test.go},
             backend/internal/repository/{document_repository.go,
             repository_test.go}
Verification: `gofmt -l .` and `go vet ./...` clean; integration suite passes
             against a disposable postgres:16-alpine (`TEST_DATABASE_URL=... go
             test ./...`), covering each verb, validation/404/409/403 cases,
             include=content, the notes tree, and activity recording. Live
             smoke test of the built binary exercised create -> get -> list ->
             update -> tree -> delete -> 404 over HTTP.
Next       : T2.2 - Tags API

--------------------------------------------------------------------------------
[2026-09-17T20:41:25Z] TASK T2.2 - Tags API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Implemented the tags API: GET /api/tags (search/sort/pagination),
             GET /api/tags/popular (usage counts, private excluded), and
             GET /api/tags/:name/documents (paginated documents by tag). Added
             TagRepository.GetVisibleByNormalized so a tag attached only to
             private documents returns 404 rather than leaking its existence.
Files      : backend/internal/handler/tag_handler.go,
             backend/internal/handler/tag_handler_test.go,
             backend/internal/repository/tag_repository.go,
             backend/internal/router/router.go
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Tag tests assert
             private-only tags never appear in list/popular, shared-tag counts
             exclude private documents, documents-by-tag returns no private
             docs, and a private-only tag 404s. Live smoke test confirmed
             list/popular/by-tag plus 400 (invalid sort) and 404 (unknown tag).
Next       : T2.3 - Markdown import API

--------------------------------------------------------------------------------
[2026-09-17T20:45:45Z] TASK T2.3 - Markdown import API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Implemented POST /api/documents/import (multipart) accepting one or
             more .md files with a target section, optional parent_id, and
             optional default tags. Added a markdown parser for YAML
             front-matter (title, sequence or comma-separated tags) and the
             first H1 fallback, returning per-file results and a summary. Title
             derivation order is front-matter -> H1 -> filename; non-markdown
             and empty files are skipped, oversized files fail per-file, and
             private imports are rejected (403). Added MAX_IMPORT_BYTES config.
Files      : backend/internal/markdown/{markdown.go,markdown_test.go},
             backend/internal/handler/import_handler.go,
             backend/internal/handler/import_handler_test.go,
             backend/internal/router/router.go,
             backend/internal/config/config.go
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Import tests cover
             front-matter title/tags, filename fallback, multi-file with
             skipped/failed counts, default tags, folder imports, and
             validation/403 cases (private, bad section, parent misuse). Live
             smoke test confirmed an uploaded .md persisted with parsed title,
             tags, and front-matter stripped from the body.
Next       : T2.4 - Global full-text search API

--------------------------------------------------------------------------------
[2026-09-17T20:52:20Z] TASK T2.4 - Global full-text search API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Added GET /api/search backed by the existing ranked repository
             query (websearch_to_tsquery over title+content, ts_headline
             snippets, additive tag matching). Validates page/page_size and
             rejects section=private with 403; empty queries return an empty
             page. Private documents are excluded regardless of session
             (PRD 3.3). Also normalized document/tag results so tags serialize
             as [] instead of null.
Files      : backend/internal/handler/search_handler.go,
             backend/internal/handler/search_handler_test.go,
             backend/internal/router/router.go,
             backend/internal/repository/document_repository.go
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Search tests prove a
             private-only term returns zero while a public term returns ranked
             hits with snippets, tag-only matches work, pagination is correct,
             empty queries are empty, and section=private is 403. Live smoke
             test confirmed ranked results and a 403 for the private section.
Next       : T2.5 - Private Archive access control

--------------------------------------------------------------------------------
[2026-09-17T20:57:23Z] TASK T2.5 - Private Archive access control
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Implemented the Private Archive vault: bcrypt master-password
             verification, in-memory sliding 15-minute sessions with opaque
             tokens, a fixed-window unlock rate limiter (5 attempts / 15 min),
             and auth middleware guarding private routes (Bearer or HttpOnly
             cookie). Added /api/private unlock, lock, and documents CRUD.
             Private-only mutations are never written to the activity feed;
             public endpoints continue to exclude private content, and private
             routes never expose public documents.
Files      : backend/internal/private/{password.go,session.go,ratelimit.go,
             private_test.go},
             backend/internal/repository/settings_repository.go,
             backend/internal/handler/{private_auth.go,private_handler.go,
             private_handler_test.go},
             backend/internal/router/router.go, backend/go.mod, backend/go.sum
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Tests cover unit
             hashing/session/rate-limit behavior plus HTTP: 401 unauthenticated,
             wrong password 401, lockout 429 (including correct password during
             lockout), successful unlock + private CRUD, cookie transport,
             lock invalidating the session, private exclusion from documents/
             search/tags/popular, and private routes 404ing public documents.
             Live smoke test exercised the full locked -> unlock -> create ->
             leak-check -> lock -> relocked flow.
Next       : T2.6 - Dashboard aggregation API

--------------------------------------------------------------------------------
[2026-09-17T21:56:36Z] TASK T2.6 - Dashboard aggregation API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Added GET /api/dashboard returning the four navigation cards with
             per-section counts and routes, recently updated documents, popular
             tags, and the recent activity feed. The private card reports
             locked state from the session without extending it (new
             SessionStore.Valid) and omits its count. All payload sections use
             the existing private-excluding repository queries.
Files      : backend/internal/handler/dashboard_handler.go,
             backend/internal/handler/dashboard_handler_test.go,
             backend/internal/handler/handler_test.go,
             backend/internal/private/session.go,
             backend/internal/router/router.go
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Dashboard tests
             assert card counts, a null private count, private exclusion from
             recently_updated/popular_tags/activity, and the locked->unlocked
             transition with a valid session. Live smoke test confirmed the
             response shape and locked:false after unlocking.
Next       : T2.7 - Activity log API

--------------------------------------------------------------------------------
[2026-09-17T22:24:00Z] TASK T2.7 - Activity log API
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Added GET /api/activity with page/page_size validation, returning
             the feed newest-first via the existing private-filtering repository
             query. Mutations across create/update/delete/import/unlock are
             already recorded by their handlers, so the endpoint surfaces the
             chronological feed with title snapshots that survive deletes.
Files      : backend/internal/handler/activity_handler.go,
             backend/internal/handler/activity_handler_test.go,
             backend/internal/router/router.go
Verification: `gofmt -l .` and `go vet ./...` clean; full integration suite
             passes against disposable postgres:16-alpine. Tests assert
             newest-first ordering (update after create), private exclusion,
             pagination across two pages, and 400 on invalid page. Live smoke
             test showed created -> updated -> deleted entries in order with
             the deleted document's title snapshot retained.
Next       : T2.8 - Backend hardening and tests

--------------------------------------------------------------------------------
[2026-09-17T22:36:23Z] TASK T2.8 - Backend hardening and tests
--------------------------------------------------------------------------------
Phase      : 2 - Backend API (Go/Gin)
Status     : DONE
Summary    : Added a middleware package (request IDs, structured access logs,
             security headers, JSON body size limits) and wired it into the
             router, replacing Gin's default logger. Centralized body-binding
             errors so oversized JSON returns 413 (payload_too_large) and
             other parse failures stay 400. Added a golangci-lint config,
             fixed every lint finding (errcheck, staticcheck), and added
             router-level integration tests exercising the fully wired engine
             plus middleware behavior.
Files      : backend/internal/middleware/{middleware.go,middleware_test.go},
             backend/internal/handler/response.go,
             backend/internal/handler/{document_handler.go,private_handler.go},
             backend/internal/router/{router.go,router_test.go},
             backend/.golangci.yml, backend/README.md,
             backend/internal/config/config.go,
             backend/internal/handler/import_handler.go,
             backend/internal/repository/{document_repository.go,
             document_repository_test.go}
Verification: `gofmt -l .`, `go vet ./...`, and `golangci-lint run ./...`
             report 0 issues; full `go test ./...` passes against disposable
             postgres:16-alpine including the new middleware and router suites.
             Live smoke test confirmed X-Request-ID and security headers,
             structured log lines, a 413 for an oversized body, and a
             subsequent 201 for a normal create.
Next       : T3.1 - Vite/React app and routing

--------------------------------------------------------------------------------
[2026-09-17T22:43:09Z] TASK T3.1 - Vite/React app and routing
--------------------------------------------------------------------------------
Phase      : 3 - Frontend Foundation
Status     : DONE
Summary    : Established the SPA shell: added react-router-dom with the full
             route map from docs/api.md section 12 (dashboard, documents,
             workflows, knowledge-base, cheat-sheets, private, search,
             settings, document viewer, editor) plus a 404 redirect. Built a
             typed API client (ApiError with error-envelope parsing, query
             builder, FormData support, credentials: include) and a shared
             types module mirroring the contract. Placeholder pages render each
             route; VITE_API_BASE_URL config already provided.
Files      : frontend/src/{App.tsx,main.tsx,routes.ts,index.css},
             frontend/src/api/{client.ts,types.ts},
             frontend/src/pages/PlaceholderPage.tsx, frontend/package.json,
             frontend/package-lock.json
Verification: `npm run lint`, `npm run format:check`, `npx tsc -b --noEmit`,
             and `npm run build` all succeed. Dev server serves / and a deep
             route (/knowledge-base) with HTTP 200 confirming SPA fallback.
Next       : T3.2 - Design system and theming

--------------------------------------------------------------------------------
[2026-09-17T22:46:48Z] TASK T3.2 - Design system and theming
--------------------------------------------------------------------------------
Phase      : 3 - Frontend Foundation
Status     : DONE
Summary    : Encoded the DESIGN.md visual language as CSS custom properties in
             styles/theme.css (background #f8f9fa, white surfaces, #212529 text,
             #6c757d secondary, lime accent #c4e349, tag bg #e9ecef, border
             #dee2e6, plus type/spacing/radius/shadow/z-index scales) and added
             styles/base.css with reset, base element styles, and small layout
             utilities. index.css now imports theme then base.
Files      : frontend/src/styles/theme.css, frontend/src/styles/base.css,
             frontend/src/index.css
Verification: `npm run lint`, `npx tsc -b --noEmit`, and `npm run build` succeed;
             the compiled CSS bundle contains the token declarations (verified
             --color-accent #c4e349 and --color-bg #f8f9fa in dist).
Next       : T3.3 - Base UI components

--------------------------------------------------------------------------------
[2026-09-17T22:54:43Z] TASK T3.3 - Base UI components
--------------------------------------------------------------------------------
Phase      : 3 - Frontend Foundation
Status     : DONE
Summary    : Built the reusable primitives from DESIGN.md 2.3-2.5 with a shared
             components.css and barrel export: Button (accent/secondary/ghost/
             danger, sizes, loading), Card, TagPill (display/clickable/active),
             Input/Textarea (label/hint/error), SearchBar (pill with inner
             green Search button), ListRow, SectionHeader, IconBadge, EmptyState,
             Avatar, Spinner, Skeleton, Modal (focus trap, Escape, overlay
             close, focus restore), and Dropdown (outside-click/Escape close).
             Added a dev-only /_gallery page rendering every component state.
Files      : frontend/src/styles/components.css, frontend/src/index.css,
             frontend/src/components/{Button,Card,TagPill,Input,SearchBar,
             ListRow,SectionHeader,IconBadge,EmptyState,Avatar,Spinner,Skeleton,
             Modal,Dropdown,index}.ts(x), frontend/src/pages/Gallery.tsx,
             frontend/src/App.tsx
Verification: `npm run lint` (0 warnings), `npm run format:check`, `npx tsc -b
             --noEmit`, and `npm run build` all succeed; dev server returns 200
             for /_gallery and transforms the component barrel correctly.
Next       : T3.4 - Layout shell: Header, Footer, responsive navigation
